# xsyncfar
Tool to one directional sync across pre-mapped directory paths with find and replace rules

## CLI (temporary)

For now, the CLI prints `helloworld`:

```bash
python -m xsyncfar
xsyncfar
```
